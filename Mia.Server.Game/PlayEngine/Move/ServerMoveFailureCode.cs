namespace Mia.Server.Game.PlayEngine.Move
{
    public enum ServerMoveFailureCode
    {
        None,
        NO_PLAYERS,
        ONLY_ONE_PLAYER,
        INVALID_NAME,
        NAME_ALREADY_TAKEN
    }
}
