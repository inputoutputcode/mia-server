namespace Mia.Server.Game.Communication.Command
{
    public enum ServerCommandCode
    {
        None,
        REGISTERED,
        REJECTED,
        INVALID_NAME,
        NAME_ALREADY_TAKEN,
    }
}
